<Page x:Class="SUP_G6.Views.HighScorePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SUP_G6.Views" xmlns:local2="clr-namespace:SUP_G6.Converter"
      mc:Ignorable="d" 
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="HighScorePage">
    <Page.Resources>
       
        <local2:HighScoreSortingConverter x:Key="HighScoreSortingConverter"/>
    

    </Page.Resources>

    <Grid>

        <Border
            Background="Black"
            CornerRadius="70"
            Width="1000"
            Height="810"
            Opacity="0.50"
            >
            


        </Border>
        <StackPanel>
            <Label Content="HIGHSCORE" HorizontalAlignment="Center" Margin="0 100 0 40" Foreground="Yellow" FontSize="60" FontWeight="Bold"/>
            <ListBox ItemsSource="{Binding  ListOfGameResults}" BorderBrush="Yellow" BorderThickness="2" Height="442" Width="630"/>
            <Label  Foreground="Yellow" FontSize="20" Content="SORT BY" HorizontalAlignment="Center"></Label>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                

            <Button Command="{Binding SortByTimeCommand}" Content="TIME" Style="{StaticResource ButtonTemplate}" Width="75" Height="40"/>
                <Button Command="{Binding SortByNameCommand}" Content="NAME" Style="{StaticResource ButtonTemplate}" Width="75" Height="40" />
                <Button Command="{Binding SortByTriesCommand}" Content="TIME" Style="{StaticResource ButtonTemplate}" Width="75" Height="40" />
            </StackPanel>
            <Button Style="{StaticResource ButtonTemplate}" Content="Back" Height="71" Width="285" Margin="0 30 0 0" Click="Button_Click"/>


        </StackPanel>

    </Grid>
</Page>
